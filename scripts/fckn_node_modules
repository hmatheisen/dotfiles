#!/bin/sh

DOCUMENTS=~/Documents

if [ -d $1 ]; then
  $DOCUMENTS = $1
fi

# Delete all node_modules directories inside ~/Documents or first argument.
find $FILE -name node_modules -type d | xargs rm -rfv
